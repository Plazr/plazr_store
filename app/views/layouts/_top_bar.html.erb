<div class="topbar-wrapper">
	<div class="container">
		<div class="topbar clearfix">
			<nav>
				<ul class="nav nav-pills pull-left">
					<% if @logo.nil?%>
						<li><%= link_to 'New Logo', plazr_store.new_admin_logo_path %></li>
					<% else %>
						<li><%= link_to 'Update Logo', plazr_store.edit_admin_logo_path(@logo) %></li>
					<% end %>
					<% if @banner.nil? %>
						<li><%= link_to 'New Banner', plazr_store.new_admin_banner_path %></li>
					<% else %>
						<li><%= link_to 'Update Banner', plazr_store.edit_admin_banner_path(@banner) %></li>
					<% end %>
					<li><%= link_to 'Store', plazr_store.index_path %></li>
					<% unless @pages.nil? %>
						<% @pages.each do |page| %>
							<li><%= link_to page.title, page %></li>
						<% end %>
					<% end %>
				</ul>
			</nav>

			<%= form_tag plazr_store.search_path, :method => 'get', :class => "form-inline pull-right" do %>

				<select>
					<option value="default">--</option>
					<option value="camisolas">Camisolas</option>
					<option value="calcas">Calças</option>
					<option value="sapatilhas">Sapatilhas</option>
				</select>

				<%= text_field_tag :search, params[:search], :placeholder => "search something", :class => "input-large" %>
				<%= submit_tag "Search", :name => nil, :class=>"btn" %>

				<%= render 'layouts/user' %>

			<% end %>
		</div>
	</div>
</div>




