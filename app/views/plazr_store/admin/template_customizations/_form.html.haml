= form_for [:admin, @template_customization] do |f|
  - if @template_customization.errors.any?
    #error_explanation
      %h2= "#{pluralize(@template_customization.errors.count, "error")} prohibited this TemplateCustomization from being saved:"
      %div{:class=>["alert","alert-error"]}
        %ul
          - @template_customization.errors.full_messages.each do |msg|
            %li= msg

  = f.label :id_name
  = f.text_field :id_name
  %br

  = f.label :display_name
  = f.text_field :display_name
  %br

  = f.label :value
  = f.text_field :value
  %br

  //%h4= "Products"
  //= f.fields_for :product_promotions do |pp|
  //  = pp.check_box :_destroy, {:checked => pp.object.product_id.to_s == @prod || @check_prods.include?(pp.object.product_id.to_s) || pp.object.persisted?}, '0', '1'
  //  = pp.label :_destroy, pp.object.product.name
  //  = pp.hidden_field :product_id
  //%br

  = f.submit "Submit"
  = link_to 'Cancel', admin_template_customizations_path
