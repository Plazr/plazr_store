= form_for [:admin, @page], :html => { :class => [:form, 'form-horizontal'] } do |f|
	/ - if @page.errors.any?
	/ 	#error_explanation
	/ 		%h2= "#{pluralize(@page.errors.count, "error")} prohibited this page from being saved:"
	/ 		%div{:class=>["alert","alert-error"]}
	/ 			%ul
	/ 				- @page.errors.full_messages.each do |msg|
	/ 					%li= msg
	.control-group
		= f.label 'Título', :class => 'control-label'
		.controls
			= f.text_field :title, :class => :span12

	.control-group
		%label.control-label http://plazr.net/storename/
		.controls
			= f.text_field :slug, :class => :span12

	.control-group
		= f.label 'Conteúdo', :class => 'control-label'
		.controls
			= f.text_area :content, :class => ['redactor', :span12], :rows => 40

	.control-group
		.controls
			= f.submit 'Gravar', :class => [:btn, 'btn-primary']
